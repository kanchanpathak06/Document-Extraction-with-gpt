resources:
  jobs:
    document_extraction:
      name: Document Extraction
      max_concurrent: 1
      tasks:
        - task_key : document_extraction_task
          job_cluster: document_extraction_cluster
          libraries:
            - pypi:
                package: pymupdf
            - pypi:
                package: dspy==2.6.2

